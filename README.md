# NASA APOD Project

## Overview

This project uses NASA's Astronomy Picture of the Day (APOD) API to retrieve information about the daily astronomy image. The data returned from the API is converted into a pandas DataFrame and stored in a SQLite database.

## Setup Instructions

### Required Libraries

Install the required Python libraries:

```bash
pip3 install requests pandas sqlalchemy